# Local demo database.
DATABASE_URL="file:./dev.db"

# Use a long random value in every environment.
SESSION_SECRET="replace-with-local-secret-at-least-32-chars"

# Optional AI providers. Leave empty if AI-backed features are disabled locally.
OPENAI_API_KEY=""
ANTHROPIC_API_KEY=""
